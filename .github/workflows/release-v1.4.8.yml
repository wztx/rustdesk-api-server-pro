name: 禁用的一次性 v1.4.8 发布流程
run-name: disabled one-off v1.4.8 workflow

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "This one-off workflow is disabled."
          echo "Use the original .github/workflows/build-release.yml with a v*.*.* tag for normal releases."
